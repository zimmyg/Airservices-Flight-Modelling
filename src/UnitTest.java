import static org.junit.Assert.assertEquals;
import org.junit.*;

public class UnitTest {
	
	
	/* Don't have these functions yet
	public void testGetFlightNumber(){
        // Unit test 1
        // This test is designed to test if the getFightNum() in Flight.java
        // returns the correct result
		Flight tester1 = new Flight();
		tester1.setFlightNumber("QA118");
		assertEquals("MUST RETURN QA118", "QA118", tester1.getFlightNum());
	}
    
    public void testGetFlightPathColour(){
        // Unit test 2
        // This test is designed to test if the getFightPathColour() in Flight.java
        // returns the correct result
        Flight tester2 = new Flight();
        tester2.setFlightPathColour("Red")
        assertEquals("MUST RETURN Red", "Red", tester2.getFlightPathColour());

    }
    
    public void testLocationLatitude(){
        // Unit test 3
        // This test is designed to test if the getLatitude() in Location.java
        // returns the correct result
        Location tester3 = new Location();
        tester3.setLatitude(10.0);
        assertEquals("MUST RETURN 10.0",10.0 , tester3.getLatitude());
    }
    */
}

